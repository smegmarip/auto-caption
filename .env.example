# Unraid Media Path
# Path to your Unraid media directory (will be mounted to /data in container)
# Production: /mnt/user/movies (or your Unraid path)
# Local dev: ./test-media
UNRAID_MEDIA_PATH=/mnt/user/movies

# Web Service Port
# Port to expose the FastAPI web service on the host
# Default: 9000
WEB_SERVICE_PORT=9000

# Whisper Configuration
# ============================================
# GPU Mode (Production/Unraid):
# WHISPER_DOCKERFILE=Dockerfile (or omit - this is default)
# DOCKER_RUNTIME=nvidia (or omit - this is default)
# WHISPER_MODEL=large-v3 (or omit - this is default)
# WHISPER_DEVICE=cuda (or omit - this is default)
# WHISPER_COMPUTE_TYPE=float16 (or omit - this is default)
#
# CPU Mode (Local Development):
# WHISPER_DOCKERFILE=Dockerfile.cpu
# DOCKER_RUNTIME=runc
# WHISPER_MODEL=base
# WHISPER_DEVICE=cpu
# WHISPER_COMPUTE_TYPE=int8
# ============================================

# Logging
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
