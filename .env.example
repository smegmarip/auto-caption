# Unraid Media Path
# Path to your Unraid media directory (will be mounted to /data in container)
# Production: /mnt/user/movies (or your Unraid path)
# Local dev: ./test-media
UNRAID_MEDIA_PATH=/mnt/user/movies

# Docker Network
# Use an existing Docker network to allow other containers to access the API
# If not specified, defaults to 'auto-caption-network' (which must be created first)
# Common examples: 'bridge', 'host', or a custom network like 'my-services'
DOCKER_NETWORK=auto-caption-network

# Web Service Port
# Port to expose the FastAPI web service on the host
# Default: 9000
WEB_SERVICE_PORT=9000

# Whisper Configuration
# ============================================
# GPU Mode (Production/Unraid):
# WHISPER_DOCKERFILE=Dockerfile (or omit - this is default)
# DOCKER_RUNTIME=nvidia (or omit - this is default)
# WHISPER_MODEL=large-v3 (or omit - this is default)
# WHISPER_DEVICE=cuda (or omit - this is default)
# WHISPER_COMPUTE_TYPE=float16 (or omit - this is default)
# WHISPER_CPU_THREADS=8 (optional - limits CPU threads for thermal management)
#
# CPU Mode (Local Development):
# WHISPER_DOCKERFILE=Dockerfile.cpu
# DOCKER_RUNTIME=runc
# WHISPER_MODEL=base
# WHISPER_DEVICE=cpu
# WHISPER_COMPUTE_TYPE=int8
# ============================================
#
# Performance Throttling (Thermal Management)
# Limit CPU threads to reduce thermal load without affecting quality.
# Useful for systems with inadequate cooling (e.g., NAS repurposed for compute).
# Set to 0 or omit to use all available CPU cores (default).
# Recommended values for Ryzen 9 3900X (12 cores/24 threads):
#   - Conservative: WHISPER_CPU_THREADS=8  (33% utilization)
#   - Moderate:     WHISPER_CPU_THREADS=12 (50% utilization)
#   - Balanced:     WHISPER_CPU_THREADS=16 (67% utilization)
# Note: Lower values = lower thermal load but longer transcription times.
WHISPER_CPU_THREADS=0

# Logging
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
