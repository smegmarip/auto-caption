# DeepL API Configuration
# Get your free API key at: https://www.deepl.com/pro-api
DEEPL_API_KEY=your_deepl_api_key_here

# Unraid Media Path
# Path to your Unraid media share (e.g., /mnt/user/media)
UNRAID_MEDIA_PATH=/mnt/user/media

# Web Service Port
# Port to expose the FastAPI web service on the host
# Using 9080 to avoid common port conflicts (8000, 8080, etc.)
WEB_SERVICE_PORT=9080

# Docker Network
# Use an existing Docker network to allow other containers to access the API
# If not specified, defaults to 'auto-caption-network' (which must be created first)
# Common examples: 'bridge', 'host', or a custom network like 'my-services'
DOCKER_NETWORK=auto-caption-network

# Logging
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
